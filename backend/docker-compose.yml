version: "3.9"
services:
  service:
    build: .
    restart: always
    ports:
      - 8080:8080
    depends_on:
      mysqldb: 
        condition: service_completed_successfully
    
  mysqldb:
    image: mysql
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_DATABASE: bolao
      MYSQL_ROOT_PASSWORD: root
